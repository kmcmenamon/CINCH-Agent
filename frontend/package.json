{
  "name": "cinch-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "mkdir -p dist && cp -r public/* dist/ 2>/dev/null || true && cp src/App.* dist/ 2>/dev/null || true"
  },
  "dependencies": {},
  "devDependencies": {}
}
